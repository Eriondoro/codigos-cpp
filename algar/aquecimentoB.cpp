#include <bits/stdc++.h>
using namespace std;
int main()
{
    set<int> seq;
    set<int>:: iterator it;
    int N;

    cin >> N;
    for(int i=0; i<N;i++){
        int M;
        cin >> M;

        seq.insert(M);
    }

    cout << seq.size() << endl;












    
    return 0;
}