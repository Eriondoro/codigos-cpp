#include <bits/stdc++.h>
using namespace std;

int main(){
    int i, n;

    cin >> i;

    for(int a=1; a<=i; a++){
        cout << a << " " << pow(a, 2) << " " << pow(a, 3) << endl;
    }

    return 0;
}